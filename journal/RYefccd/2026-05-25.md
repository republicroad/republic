### shadcn admin

1.  前后端 api spec 设计
	-  正常返回
	-  异常返回
2. 前端 api, service 与 mockHandler 的设计
	- api 集中配置和管理
	- 使用 service 包装 api axios 的使用. 避免在组件中硬编码 http 请求相关代码
	- 怎么对 endpoint 建模，使之易于使用 msw 来 mock 数据
	- service 层中应当包含 mock 么
	- 考虑使用 openapi 生成的 typescript sdk, 提供更好的代码提示，错误提示等概念
3.  tanstack query 要查看请求是否携带鉴权的 bear token.
4. 设计 team 或者 workspace 等组织功能.
5. 设计关于规则的生命周期和属性
6. forgot-password 使用 zustand 来保持多步骤的表单之间的状态.



### 需求设计

1. 根据自定义函数规范自动生成自定义节点.
2. 修复 jdm-eidotr 中 js function 节点右边内置 js 库的图标尺寸问题.
3. 将brde后台中的规则管理的子菜单功能转移到场景管理中.
4. 重新设计规则管理的相关菜单和功能
5. 从服务端返回 auth.json 相关的权限模式需要去掉. 服务端只返回对应的角色, 前端根据角色渲染不同的界面.
6.  brde 后台登录成功时顺便写入新的 password 字段. 