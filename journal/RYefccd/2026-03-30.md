
### git 补丁


> git apply crypto.randomUUID.patch

如果未能打补丁，则使用 --reject 参数

> git apply --reject crypto.randomUUID.patch


### git 设置仓库用户

> git config --local user.email 847960106@qq.com
> git config --local user.name  RYefccd


### 规则引擎思考

dmn 是 bpmn 的流程编排用于决策的补充。
工作流和决策能否重新整体设计，zen engine 目前已经超过了 dmn 概念的外延。能否重新去考虑工作流和决策的实现。因为某种意义上，决策和工作流在某些场景会成为彼此的依赖条件或者依赖触发动作。